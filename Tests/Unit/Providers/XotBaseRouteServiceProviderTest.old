<?php

declare(strict_types=1);

namespace Tests\Unit\Modules\Xot\Providers;

use Modules\Xot\Providers\XotBaseRouteServiceProvider;
use Tests\TestCase;

/**
 * Class XotBaseRouteServiceProviderTest.
 *
 * @covers \Modules\Xot\Providers\XotBaseRouteServiceProvider
 */
final class XotBaseRouteServiceProviderTest extends TestCase
{
    private XotBaseRouteServiceProvider $xotBaseRouteServiceProvider;

    protected function setUp(): void
    {
        parent::setUp();

        /* @todo Correctly instantiate tested object to use it. */
        $this->xotBaseRouteServiceProvider = $this->getMockBuilder(XotBaseRouteServiceProvider::class)
            ->setConstructorArgs([])
            ->getMockForAbstractClass();
    }

    protected function tearDown(): void
    {
        parent::tearDown();

        unset($this->xotBaseRouteServiceProvider);
    }

    public function testBoot(): void
    {
        /* @todo This test is incomplete. */
        self::markTestIncomplete();
    }

    public function testMap(): void
    {
        /* @todo This test is incomplete. */
        self::markTestIncomplete();
    }
}
